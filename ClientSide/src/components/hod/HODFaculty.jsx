import React from 'react';
import { Box, Typography } from '@mui/material';

const HODFaculty = () => (
  <Box sx={{ p: 4 }}>
    <Typography variant="h5" fontWeight={700} gutterBottom>Faculty</Typography>
    <Typography color="text.secondary">(Coming Soon) View and manage faculty here.</Typography>
  </Box>
);

export default HODFaculty;
